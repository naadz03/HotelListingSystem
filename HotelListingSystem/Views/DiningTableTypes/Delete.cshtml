@model HotelListingSystem.Models.DiningTableTypes

@{
    ViewBag.Title = "Delete";
    Layout = null;
}
<head>
    <style>
        hr {
            width:30%;
        }
    </style>
    @Html.Partial("_eHead")
</head>

<body>
    <main>
        <div class="container_body">
            <h2>Delete</h2>

            <h5>Are you sure you want to delete this?</h5>
            <div>
                
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.Name)
                    </dt>

                    <dd>
                        @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control col-md-3", disabled = "disabled" } })
                        @*@Html.DisplayFor(model => model.Name)*@
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-outline-danger" /> |
                        @Html.ActionLink("Back to List", "Index", new { id = 0 }, new { @class = "btn btn-primary" })
                    </div>
                }
            </div>
        </div>
    </main>
</body>
@Html.Partial("_eFooter")
